#!/usr/bin/env sh
set -eu
msg_file="$1"
if ! grep -Eq '^(feat|fix|docs|test|refactor|chore|ci|build)(\([a-z0-9._-]+\))?: .{1,72}$' "$msg_file"; then
  echo "Commit message must match: type(scope): summary"
  echo "Allowed types: feat, fix, docs, test, refactor, chore, ci, build"
  exit 1
fi
